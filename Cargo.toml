[package]
name = "fix_akonadi"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4.42"
regex = "1.12.2"
sqlx = { version = "0.8.6", features = [ "mysql", "runtime-tokio-native-tls", "macros" ] }
tokio = { version = "1", features = [ "full" ] }
walkdir = "2"
